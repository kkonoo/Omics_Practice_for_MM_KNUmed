{
  "hash": "721275b7c51c576b83b2f3cb335248c4",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"오믹스 데이터 분석 기술 Day 2\"\nsubtitle: \"의학과 4학년 1학기 - 분자의학 선택연구\"\nauthor: \"하은지 (분자의학교실)\"\ndate: last-modified\nformat:\n  html\nexecute:\n  echo: true\n  eval: false\n---\n\n# Today's lecture\n\n![](/assets/img/day2.png){fig-align=\"center\"}\n\n# 1. EDA (explantory data analysis)\n\n## 1) why do we need EDA?\n\nVisualize data [before statistical analysis]{.underline} to **understand its overall characteristics and quality**\n\n-   **Quality Control**: identify outlier samples\n-   **Batch Effect Detection**: check whether technical noise (experiment date, instrument, etc.) is masking biological signal\n-   **Heteroscedasticity**: variance increases with expression level → a small number of highly expressed genes can dominate the entire analysis\n\n------------------------------------------------------------------------\n\n## 2) Data Transformation: vst vs. rlog\n\nwe need to transform our data (RNA-seq data) for statistic analyses\n\n![](/assets/img/DEG/trans.png){fig-align=\"center\"}\n\n💡 vst/rlog transformed values are **for visualization only**; raw counts must be used for differential analysis!\n\n------------------------------------------------------------------------\n\n### ① vst\n\n> Variance Stabilizing Transformation\n\n::::: columns\n::: {.column width=\"50%\"}\n-   Suitable for n ≥ 30 (large sample sizes)\n-   Based on a global trend → fast\n-   `asinh` transformation for highly-expressed genes\n:::\n\n::: {.column width=\"50%\"}\n![](/assets/img/DEG/vst.png){fig-align=\"center\"}\n:::\n:::::\n\n------------------------------------------------------------------------\n\n### ② rlog\n\n> regularized Log\n\n::::: columns\n::: {.column width=\"50%\"}\n-   Suitable for n \\< 30 (small sample sizes)\n-   Per-gene shrinkage (Bayesian estimation) → slow\n-   Suppresses noise in low-count genes\n:::\n\n::: {.column width=\"50%\"}\n![](/assets/img/DEG/rlog.png){fig-align=\"center\"}\n:::\n:::::\n\n💡 Imagine this\n\n```         \ngene A (low):\nsample 1: 1, sample 2: 5 ➡️ logFC = log2(5/1) = 2.32 … noise ↑\n\ngene B (high):\nsample 1: 1000, sample 2: 1004 ➡️ logFC = log2(1004/1000) = 0.005 … noise ↓ (stabilized)\n```\n\n------------------------------------------------------------------------\n\n### ③ Practice \\| DESeq2::vst()\n\n> download practice data and save the following files in `data/` in your R project\n\n| File | Link |\n|-------------------------|-----------------------------------------------|\n| `0_raw_counts_final.rds` | [📥 Download](https://drive.google.com/file/d/1y6rBloxvp5c4iYr7AVqVjUrpgZiBaSXR/view?usp=drive_link) |\n| `0_meta_data_final.rds` | [📥 Download](https://drive.google.com/file/d/1-vi09nduyHYfKIGTWkfdvcRVyzIh3WED/view?usp=drive_link) |\n| `1_1_dds.rds` | [📥 Download](https://drive.google.com/file/d/1noV8OAt2t5cbRceGDe1oUgY-nJBbxvNF/view?usp=drive_link) |\n| `1_2_dds_filtered.rds` | [📥 Download](https://drive.google.com/file/d/1pZukVok9fwXjCe-LhbD-EJ_i98g526_5/view?usp=drive_link) |\n| `1_3_vsd.rds` | [📥 Download](https://drive.google.com/file/d/1FD6KadX9vYeTMeoayBcv0ejMmaxDdQeH/view?usp=drive_link) |\n\n### i. load data (LUAD data from TCGA, n=58\\*2)\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(tidyverse)    # dplyr, ggplot2 등 포함\nlibrary(DESeq2)\n\n# set your wd or open your R project\nsetwd(\"T:/homes/eha/교육/[학부] 4-1 분자의학 선택연구/2026/\")\n\n# 1. read data (count table and metadata) ####\ncountData <- readRDS('data/0_raw_counts_final.rds')\ndim(countData)\nhead(countData[,1:5])\n\ncolData <- readRDS(\"data/0_meta_data_final.rds\")\nhead(colData)\ntable(colData$tissue_type)\n\n# check sample ids of the count table and metadata\nall(colnames(countData) == colData$barcode) # should be TRUE\n\n# set the Factor level (condition)\n# default: alphabetical ... ref = Control/Normal\ncolData$condition <- factor(colData$tissue_type, levels = c(\"Normal\", \"Tumor\")) \ncolData$condition\n# case / control ... treatment / control ... KO / control\n\n\n# 2. DESeq2 object (dds) ####\n# countData, colData, design\ndds <- DESeqDataSetFromMatrix(countData = countData,\n                              colData = colData,\n                              design = ~ patient + condition) # DEG by condition\ncolData$patient = gsub('-','_',colData$patient)\ndds\nsaveRDS(dds, file='data/1_1_dds.rds')\n```\n:::\n\n\n### ii. pre-filtering\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 3. Pre-filtering (de-noising) ####\ndim(counts(dds))\n\nhead(counts(dds)[,1:4])\nhead(counts(dds)[,1:4]>=1)\nhead(rowSums(counts(dds)[,1:4]>=1))\nhead(rowSums(counts(dds)>=1))\nrowSums(counts(dds)) %>% head\n\n## 3-1. lowly expressed genes (for speed-up & statistical power) ####\n### option 1: simple (Total Count < 10?) ####\nkeep <- rowSums(counts(dds)) >= 10\nhead(keep)\ncat(paste(\"   Removed\", sum(!keep), \"low count genes.\\n\"))\ncat(paste(\"   Remaining genes:\", sum(keep), \"\\n\"))\ndds_filtered <- dds[keep, ]\n\n### option 2: considring group size (recommended) ####\n# Total Count < 10 in the smallest group?\n# ex: Control(3), Case(10) -> non-zero counts in at least 3 samples?\nmain_condition = 'condition'\nsmallestGroupSize <- min(table(colData[[main_condition]]))\nsmallestGroupSize*0.4\nkeep <- rowSums(counts(dds) >= 1) >= (smallestGroupSize*0.5)\ncat(paste(\"   Removed\", sum(!keep), \"low count genes.\\n\"))\ncat(paste(\"   Remaining genes:\", sum(keep), \"\\n\"))\ndds_filtered <- dds[keep, ]\n\n## 3-2. confounding genes ####\nall_genes <- rownames(dds_filtered)\nhead(all_genes)\n# Mitochodiral: starting with \"^MT-\" \nmt_genes <- grep(\"^MT-\", all_genes, value = TRUE)\nmt_genes\n# Ribosomal: starting with \"^RPS|^RPL\"\nribo_genes <- grep(\"^RPS|^RPL\", all_genes, value = TRUE)\nribo_genes\n# Immunoglobulin: starting with \"^IG[HKL]\" : \"IGH\", \"IGK\", \"IGL\"\nig_genes <- grep(\"^IG[HKL]\", all_genes, value = TRUE)\nig_genes\n# filtering gene list\ncontam_list <- c(mt_genes, ribo_genes, ig_genes)\nlength(contam_list)\n# !: 'NOT'\nkeep_indices <- !(all_genes %in% contam_list)\ndds_filtered <- dds_filtered[keep_indices, ]\ndds_filtered\n\nsaveRDS(dds_filtered, file='data/1_2_dds_filtered.rds')\n```\n:::\n\n\n### iii. data transformation\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 4. Variance Stabilizing Transformation (vst) ####\n## 4-1. vst (normalization and transformation) ####\nvsd <- vst(dds_filtered, blind=FALSE) \n# blind = FALSE: using design (recommended), blind=TRUE: using only technical noises\nsaveRDS(vsd, file='data/1_3_vsd.rds')\n\n# check the result visually\nvsd_data = assay(vsd)\nhead(vsd_data[,1:4]) # similar to log-scale\ndf_vst <- data.frame(mean = rowMeans(vsd_data),\n                     sd = apply(vsd_data, 1, sd)\n                     )\nhead(df_vst)\n\nggplot(df_vst, aes(x = mean, y = sd)) +\n  geom_point(alpha = 0.1) +\n  geom_smooth(color = \"red\") + # trend line\n  theme_minimal() +\n  labs(title = \"Variance Stabilization Check (VST)\",\n       x = \"Mean Expression\", y = \"Standard Deviation\")\n\n# no transformation\ncount_data = assay(dds_filtered)\ndf_count <- data.frame(mean = rowMeans(count_data),\n                       sd = apply(count_data, 1, sd)\n)\nggplot(df_count, aes(x = mean, y = sd)) +\n  geom_point(alpha = 0.1) +\n  geom_smooth(color = \"red\") + #  trend line\n  theme_minimal() +\n  labs(title = \"Variance Stabilization Check (no transf)\",\n       x = \"Mean Expression\", y = \"Standard Deviation\")\n```\n:::\n\n\n![](/assets/img/DEG/W6_vst_trans.png)\n\n------------------------------------------------------------------------\n\n## 3) Visual Exploration\n\nWhat to check through visualization:\n\n-   Is there a difference between groups?\n-   Are there any outlier samples?\n-   Is there a batch effect?\n\n![](/assets/img/DEG/EDA.png){fig-align=\"center\"}\n\n------------------------------------------------------------------------\n\n### PCA\n\n> principal component analysis\n\n![](/assets/img/DEG/W6_PCA_theory.png){fig-align=\"center\"}\n\n\n::: {.cell}\n\n```{.r .cell-code}\n## 5-1. PCA plot ####\npca_res <- prcomp(t(vsd_data))\npca_res$x %>% head\n\n# PC1 and PC2\npca_coords <- as.data.frame(pca_res$x)[,1:2]\npca_coords %>% head\n\nggplot(pca_coords, aes(x=PC1, y=PC2)) + \n  geom_point(size=3) + ggtitle(\"PCA Result\") + theme_bw()\n\n\n## 5-2. using DESeq2 function (using top 100) ####\nplotPCA(vsd, intgroup = \"condition\") +\n  theme_classic() +\n  labs(title = \"PCA Plot\")\n\nplotPCA(vsd, ntop=nrow(vsd), intgroup = \"condition\") +\n  theme_classic() +\n  labs(title = \"PCA Plot\")\n```\n:::\n\n\n![](/assets/img/DEG/W6_PCA.png){fig-align=\"center\"}\n\n------------------------------------------------------------------------\n\n# 2. DEG analysis\n\n## 1) DEG\n\n> differentially expressed genes; genes whose expression levels change significantly between different conditions (e.g., Case vs. Control)\n\n![](/assets/img/DEG/W7_DEG.png){fig-align=\"center\"}\n\n💡Not t-test!\n\n> doesn't work for RNA-seq data\n\n![](/assets/img/DEG/trans.png){fig-align=\"center\"}\n\n## 2) linear model\n\n![](/assets/img/DEG/lm.png){fig-align=\"center\"}\n\n::::: columns\n::: {.column width=\"38%\"}\n![](/assets/img/DEG/W7_LM_var.png){fig-align=\"center\" width=\"740\"}\n:::\n\n::: {.column width=\"62%\"}\n![](/assets/img/DEG/W7_LM.png){fig-align=\"center\"}\n:::\n:::::\n\n------------------------------------------------------------------------\n\n### Variance decomposition\n\n> \"Statistics is the process of tracing the source of variation.\"\n\n$$ Total Variance (RNA) = \\mathbf{Condition} + \\mathbf{Batch} + Residual $$\n\n![](/assets/img/DEG/var.png){fig-align=\"center\"}\n\n------------------------------------------------------------------------\n\n## 3) statistical model of DESeq2\n\n### ① Characteristics of RNA-seq Data\n\n-   **Count data**: discrete integers including zero\n-   **Overdispersion**: variance \\>\\>\\> mean → standard linear regression is not appropriate\n\n![](/assets/img/DEG/NB.png){fig-align=\"center\"}\n\n-   **Negative Binomial Distribution**\n\n$$ Var(\\mu) = \\mu + \\alpha\\mu^2 $$\n\n$\\mu$: mean expression (intrinsic sampling error)\\\n$\\alpha\\mu^2$: overdispersion (variation across biological replicates)\n\n------------------------------------------------------------------------\n\n### ② GLM\n\n> Generalized Linear Model\n\n$$ \\log(\\mu) = \\beta_0 + \\beta_1(condition) + \\beta_2(batch) + \\cdots $$\n\n::::: columns\n::: {.column width=\"38%\"}\n![](/assets/img/DEG/GLM.png){fig-align=\"center\"}\n:::\n\n::: {.column width=\"62%\"}\n![](/assets/img/DEG/GLM2.png){fig-align=\"center\"}\n:::\n:::::\n\n------------------------------------------------------------------------\n\n### ③ Estimating Dispersion α (Shrinkage)\n\nAddresses inaccurate per-gene α estimation in small samples (e.g., n=3):\n\n1.  **Gene-wise**: calculate the raw dispersion for each gene\n2.  **Trend line**: fit a global trend line across all genes\n3.  **Shrink**: pull noisy estimates toward the trend line → improved accuracy, reduced false positives\n\n![](/assets/img/DEG/W7_dispersion.png){fig-align=\"center\"}\n\n------------------------------------------------------------------------\n\n### ④ LFC and significance\n\n-   **β₁(condition)** = log₂FoldChange (case/control)\n    -   Control: X=0 → log(μ_control) = β₀\n\n    -   Case: X=1 → log(μ_case) = β₀ + β₁\n\n        ∴ β₁ = log₂(μ_case / μ_control)\n-   **Wald Test**: $z = \\frac{\\beta_1}{SE(\\beta_1)}$ → p-value\n-   **LRT (Likelihood Ratio Test)**: used for time-series or ≥3 group comparisons (χ²-based)\n\n## 4) design matrix\n\n![](/assets/img/DEG/W7_design.png){fig-align=\"center\"}\n\n$$ design = ~ Batch + Confounders + Condition $$\n\n-   Condition: always in the last\n-   reference for β (LFC): X=0 for control (reference), X=1 for case\n\n### Design Matrix (Advanced)\n\n| Analysis Type | Clinical Scenario | Design Formula |\n|------------------|-----------------------------|--------------------------|\n| Paired sample | Tumor vs. Normal, Pre vs. Post | `~ Patient + Treatment` |\n| Interaction Term | Drug response × disease subgroup | `~ Disease + Drug + Disease:Drug` |\n| Time-Series | Multiple time points | `~ Time` (use LRT) |\n| Random Effect | Multiple tissues from the same individual | Not supported in DESeq2 → use `limma-voom` |\n| Hidden batch (sva) | Unmeasured technical/biological factors | `~ Age + Gender + SV1 + SV2 + Condition` |\n\n------------------------------------------------------------------------\n\n## 5) Practice \\| DESeq2()\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 1. read data (dds) ####\ndds <- readRDS('data/1_2_dds_filtered.rds')\ndds@colData %>% head\ndds@design\n\n\n# 2. DESeq2 running ####\n# Dispersion estimation -> GLM Fitting -> Wald Test\n# Simple model: ~ condition\n# Paired model: ~ patient + condition (환자별 변동을 보정)\n#               → 더 정밀한 분석!\n\ndds <- DESeq(dds)\n# ① size factor 계산 (normalization)\n# ② dispersion 추정\n# ③ GLM fitting\n# ④ Wald test (통계 검정)\nsaveRDS(dds, file='data/2_1_dds_DESeq2.rds')\n\n\ndds@design = as.formula('~ condition')\ndds_smpl <- DESeq(dds)\nsaveRDS(dds_smpl, file='data/2_2_dds_DESeq2_simple.rds')\n\n\n# 3. check Dispersion Plot ####\ndds <- readRDS('data/2_1_dds_DESeq2.rds')\nplotDispEsts(dds)\n\ndds_smpl <- readRDS(file='data/2_2_dds_DESeq2_simple.rds')\nplotDispEsts(dds_smpl)\n```\n:::\n\n\n------------------------------------------------------------------------\n\n# 3. Visualization of the DEG results\n\n## 1) Significance of DEG Results\n\n### ① multiple testing problem\n\n> Gene A showed no difference between groups (H0)\n\n-   **p-value**: the probability of observing the result [by chance]{.underline} under the null hypothesis (H₀: no difference between groups)\n-   **multiple testing problem**: testing 10,000 genes at p \\< 0.05 would yield \\~500 spuriously significant results → dramatic increase in false positives\n\n![](/assets/img/postDEG/W8_multiple.png){fig-align=\"center\"}\n\n### ② Multiple Testing Correction\n\n![](/assets/img/DEG/multiple.png){fig-align=\"center\"}\n\n#### Benjamini-Hochberg (BH) Procedure\n\n> \"Stricter for higher ranks, more lenient for lower ranks\"\n\n$$\nFDR \\leq \\frac{n_{total} \\cdot p_{cut}}{i} \\leq \\alpha(0.05)\n$$\n\n$$\np_{cut} \\leq \\frac{i}{n_{total}} \\cdot \\alpha\n$$\n\n-   **adjusted p-value (padj) ...** (with cumulative minimum applied) $$\n    p \\times \\frac{n_{total}}{i} \n    $$\n\n\n::: {.cell}\n\n```{.r .cell-code}\n##| eval: true\n \n# FYI. FDR ####\n## 1) example data (5 genes) ####\npvalues <- c(0.04, 0.001, 0.05, 0.0001, 0.2)\ngenes <- c(\"GeneA\", \"GeneB\", \"GeneC\", \"GeneD\", \"GeneE\")\ndf <- data.frame(Gene=genes, P_raw=pvalues)\ndf\n\n## 2) sorting p-values ####\ndf <- df[order(df$P_raw), ] \ndf\n\n## 3) ranking * BH cutoff (m=5, alpha=0.05) ####\nm <- 5\nalpha <- 0.05\ndf$Rank <- 1:m\ndf$BH_Threshold <- (df$Rank / m) * alpha\ndf\n\n## 4) pass or not ####\ndf$Pass <- df$P_raw <= df$BH_Threshold\ndf\n\n## 5) padj ####\ndf$padj_calc <- df$P_raw * (m / df$Rank)\ndf\ndf$padj_calc <- pmin(df$padj_calc, 1)\ndf\n\n# Cumulative Minimum from bottom\ndf$padj_final <- rev(cummin(rev(df$padj_calc)))\ndf\n\ndf$R_function <- p.adjust(df$P_raw, method=\"BH\")\ndf #%>% select(Gene, P_raw, Rank, BH_Threshold, padj_final)\n```\n:::\n\n\n### ③ Independent Filtering (DESeq2)\n\n-   problem: low count genes  hard to get statistical significance\n-   solution: pre-filtering of the low count genes –\\> total tests n 🔽 –\\> statistical power 🔼\n-   Cases where padj is NA in `DESeq2::results()`:\n    1.  Low-count gene (excluded by independent filtering)\n    2.  Outlier gene (based on Cook's distance)\n\n![](/assets/img/postDEG/W8_independent.png){fig-align=\"center\" width=\"392\"}\n\n### ④ Practice \\| DESeq2::results()\n\n> download practice data and save the following files in `data/` in your R project\n\n| File | Link |\n|-------------------------|-----------------------------------------------|\n| `2_1_dds_DESeq2.rds` | [📥 Download](https://drive.google.com/file/d/1Q4HhyfdL9HmKyvwYuVxRrN8BvJYJaOFn/view?usp=drive_link) |\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 0. load DESeq data ####\ndds <- readRDS('data/2_1_dds_DESeq2.rds')\ndds@design\ndds$condition\n\n# 1. Extracting Results ####\n# Contrast 사용 (분석 후 지정)\nres <- results(dds, contrast=c(\"condition\", \"Tumor\", \"Normal\"))\nhead(res)\n# 결과 구조 이해\n# | 열            | 의미                                   |\n# | baseMean      | 전체 샘플의 평균 발현량                |\n# | log2FoldChange| log2(Tumor / Normal) = LFC             |\n# | lfcSE         | LFC의 표준오차                         |\n# | stat          | Wald test statistic                    |\n# | pvalue        | 원래 p-value                           |\n# | padj          | FDR 보정된 p-value (adjusted p-value)  |\nsummary(res)\n\n# another option: Factor level 설정 (미리 설정)\ndds@colData$condition\ndds@colData$condition = relevel(dds@colData$condition, ref='Normal')\ndds@colData$condition\ndds@colData$condition = relevel(dds@colData$condition, ref='Tumor')\n\n## 1-2. Independent Filtering ####\n# x: Mean Normalized Counts, y: number of the significant\nplot(metadata(res)$filterNumRej, \n     type=\"b\", ylab=\"Number of rejections\", xlab=\"Quantiles of filter\")\nlines(metadata(res)$lo.fit, col=\"red\")\nabline(v=metadata(res)$filterTheta, lty=3)\ntitle(\"Independent Filtering Threshold Optimization\")\n\nres %>% as_tibble() %>% arrange(padj) %>% tail\nres %>% as_tibble() %>% filter(is.na(padj)) %>% dim\n```\n:::\n\n\n## 2) Visualization of DEG Results\n\n### ① MA Plot (Global View)\n\n::::: columns\n::: {.column width=\"50%\"}\n![](/assets/img/postDEG/W8_MA.png){fig-align=\"center\"}\n:::\n\n::: {.column width=\"50%\"}\n![](/assets/img/postDEG/W8_MA_2.png){fig-align=\"center\"}\n:::\n:::::\n\n-   X-axis: Mean Average (A), Y-axis: Log Ratio (M = log₂FC)\n-   Check for data bias and LFC inflation in low-count genes\n-   Should be symmetric around Y=0\n-   `apeglm::lfcShrink()`: controls LFC in low-count genes (recommended)\n\n\n::: {.cell}\n\n```{.r .cell-code}\n##| eval: false\n\n## 2-1. MA plot ####\n# X축: Mean Expression (평균 발현량)\n# Y축: Log2 Fold Change\n# → 전체 데이터의 분포와 bias 확인\nplotMA(res, ylim=c(-5,5), main=\"Before Shrinkage\")\n\n### LFC Shrinkage to control low count genes ####\nresultsNames(dds)\nresLFC <- lfcShrink(dds, coef=\"condition_Tumor_vs_Normal\", type=\"apeglm\")\nsummary(resLFC)\nsaveRDS(resLFC, file='data/3_dds_DESeq2_shr.rds')\n\nres_df <- as.data.frame(resLFC) %>% \n  rownames_to_column(\"gene_id\") %>%\n  mutate(is_significant = padj < 0.05 & abs(log2FoldChange) > 1)\n\n# check the shrinkage effect\npar(mfrow=c(1,2))\nplotMA(res, main=\"Before Shrinkage\", ylim=c(-5,5))\nplotMA(resLFC, main=\"After Shrinkage (Recommended)\", ylim=c(-5,5))\npar(mfrow=c(1,1))\n```\n:::\n\n\n### ② Volcano Plot (Selection)\n\n![](/assets/img/postDEG/W8_vol.png){fig-align=\"center\"}\n\n-   X-axis: log₂(Fold Change), Y-axis: −log₁₀(adjusted p-value)\n-   Identify genes with large \\|LFC\\| and low p-value\n\n\n::: {.cell}\n\n```{.r .cell-code}\n##| eval: false\n\n## 2-2. volcano ####\np_thr=0.05\nlfc_thr = 1\nvol_data <- res_df %>%\n  mutate(diffexpressed = case_when(\n    log2FoldChange > lfc_thr & padj < p_thr ~ \"UP\",\n    log2FoldChange < -lfc_thr & padj < p_thr ~ \"DOWN\",\n    TRUE ~ \"NO\"\n  ))\nhead(vol_data)\n\nggplot(vol_data, aes(x=log2FoldChange, y=-log10(padj), color=diffexpressed)) +\n  geom_point(alpha=0.5, size=1.5) +\n  scale_color_manual(values=c(\"DOWN\"=\"steelblue\", \"NO\"=\"grey\", \"UP\"=\"indianred\")) +\n  geom_vline(xintercept=c(-lfc_thr, lfc_thr), linetype=\"dashed\") +\n  geom_hline(yintercept=-log10(p_thr), linetype=\"dashed\") +\n  # labeling the top 10\n  geom_text_repel(data=head(arrange(vol_data, padj), 10), \n                  aes(label=gene_id), color=\"black\") +\n  theme_minimal() +\n  labs(title=\"Volcano Plot\", x=\"Log2 Fold Change\", y=\"-Log10 Adjusted P-value\")\n\n## using lfc (recommended)\nvol_data2 <- resLFC %>% as.data.frame() %>% mutate(gene_id=rownames(.))  %>% arrange(padj) %>%\n  mutate(diffexpressed = case_when(\n    log2FoldChange > lfc_thr & padj < p_thr ~ \"UP\",\n    log2FoldChange < -lfc_thr & padj < p_thr ~ \"DOWN\",\n    TRUE ~ \"NO\"\n  ))\nhead(vol_data2)\n\nggplot(vol_data2, aes(x=log2FoldChange, y=-log10(padj), color=diffexpressed)) +\n  geom_point(alpha=0.5, size=1.5) +\n  scale_color_manual(values=c(\"DOWN\"=\"steelblue\", \"NO\"=\"grey\", \"UP\"=\"indianred\")) +\n  geom_vline(xintercept=c(-lfc_thr, lfc_thr), linetype=\"dashed\") +\n  geom_hline(yintercept=-log10(p_thr), linetype=\"dashed\") +\n  # labeling the top 10\n  geom_text_repel(data=head(arrange(vol_data2, padj), 10), \n                  aes(label=gene_id), color=\"black\") +\n  theme_minimal() +\n  labs(title=\"Volcano Plot (shr)\", x=\"Log2 Fold Change\", y=\"-Log10 Adjusted P-value\")\n```\n:::\n\n\n### ③ Heatmap (Pattern)\n\n![](/assets/img/postDEG/W8_heatmap.png){fig-align=\"center\"}\n\n-   Visualize expression patterns of top significant genes\n-   Inspect patterns between Case/Control and identify outlier samples\n-   Apply **vst/rlog transformation → z-score scaling**\n\n\n::: {.cell}\n\n```{.r .cell-code}\n##| eval: false\n\n## 2-3. heatmap ####\n### 1) vst matrix ####\nvsd <- readRDS('data/1_3_vsd.rds')\n\n### 2) top 20 ####\ntop_genes <- res_df %>% \n  filter(padj < 0.05) %>%\n  arrange(desc(abs(log2FoldChange))) %>%\n  head(20) %>% \n  .$gene_id\nhead(top_genes)\n\n### 3) vst matrix ####\nmat <- assay(vsd)[top_genes, ]\nhead(mat)\n\n## 4) annotations ####\ndf <- as.data.frame(colData(dds)[,c(\"gender\",\"condition\")])\nhead(df)\n\n# Z-score scaling\npheatmap(mat, \n         scale=\"row\",                    # z-score\n         show_rownames=TRUE,             # label gene names\n         show_colnames=FALSE,            # label sample names\n         annotation_col=df,              # sample annotation  \n         cluster_cols = TRUE,            # sample clustering\n         main = \"Top 20 DEGs Heatmap\"    # plot title\n         )\n```\n:::\n\n\n------------------------------------------------------------------------\n\n# 4. GSEA\n\n> gene set enrichment analysis\n\n## 1) functional profiling\n\nSo far, we identified \\~5,500 DEGs (Tumor vs. Normal) with FDR≤0.05 & \\|LFC\\|≥1 … So What?\n\n![](/assets/img/postDEG/W8_vol.png){fig-align=\"center\"}\n\n–\\> “Do they [functionally cluster]{.underline} in specific biological processes?“ ... **Enrichment**\n\n-   Over-Representation Analysis (ORA)\n-   **Gene Set Enrichment Analysis (GSEA)**\n\n## 2) databases (gene sets)\n\n| Database | Characteristics | Typical Use Case |\n|------------------------|------------------------|------------------------|\n| [KEGG](https://www.genome.jp/kegg/) | Metabolic and disease pathways | Metabolic disease, signaling cascades |\n| [Gene Ontology(GO)](https://geneontology.org/) | Hierarchical gene function classification | Most widely used across all fields |\n| [Reactome](https://reactome.org/) | Peer-reviewed, manually curated pathways | Detailed biochemical mechanisms |\n| [MSigDB](https://www.gsea-msigdb.org/gsea/msigdb) / [**Enrichr**](https://maayanlab.cloud/Enrichr/#libraries) | 225+ diverse gene set collections | Exploratory analyses |\n\n![](/assets/img/postDEG/W9_enrichr.png){fig-align=\"center\"}\n\n## 3) principle of GSEA\n\n### ① input\n\n> ranked genes + gene sets\n\n![](/assets/img/postDEG/ranked.png){fig-align=\"center\"}\n\n### ② measure of Enrichment Score (ES)\n\n> for each gene set\n\n![](/assets/img/postDEG/ES.png){fig-align=\"center\"}\n\npatterns of ES\n\n![](/assets/img/postDEG/W10_pattern.png)\n\n### ③ how to get significance\n\n> permutation\n\n![](/assets/img/postDEG/permut.png){fig-align=\"center\"}\n\n## 4) Practice \\| clusterProfiler\n\n> download practice data and save the following files in `data/` in your R project\n\n| File | Link |\n|------------------------------------------------|-------------------------|\n| `4_1_res_DESeq2_with_entrez_and_rank.rds` | [📥 Download](https://drive.google.com/file/d/1A48HbcJHhQTEUDS-8Dp4iOCWCWMDx-L0/view?usp=drive_link) |\n\n### ① pre-processing\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 1. load results (with entrez_id) ####\nres_df <- readRDS('data/4_1_res_DESeq2_with_entrez_and_rank.rds')\nhead(res_df)\n\n## 1-1. input: ranked gene list ####\ncor(res_df$rank_lfc, res_df$rank_pi)\ncor(res_df$rank_lfc, res_df$rank_wald)\ncor(res_df$rank_pi, res_df$rank_wald)\n\n### (optional) removing duplicates\ndim(res_df)\nres_df <- res_df %>% distinct(entrez, .keep_all = TRUE) %>% filter(!is.na(entrez))\ndim(res_df)\n\n## 1-2. ranked gene list ####\ngeneList <- res_df$rank_wald\nnames(geneList) <- res_df$entrez \ngeneList <- sort(geneList, decreasing = TRUE)\n\nhead(geneList) # top up-regulated\ntail(geneList) # top down-regulated\n\nnames(geneList) <- res_df$gene \ngeneList <- sort(geneList, decreasing = TRUE)\n\nhead(geneList) # top up-regulated\ntail(geneList) # top down-regulated\n```\n:::\n\n\n### ② GSEA\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 2. GSEA ####\n## Gene ontology (GO) ####\ngsea_res <- gseGO(geneList = geneList,\n                  OrgDb        = org.Hs.eg.db,\n                  keyType = \"SYMBOL\",\n                  ont          = \"BP\",            \n                  minGSSize    = 10,              # excluding too small pathways\n                  maxGSSize    = 500,             # excluding too large pathways\n                  pvalueCutoff = 1,               # pvalue cutoff to show terms\n                  by = \"fgsea\" )                  # fgsea: pvalues by tail approximate (fast), DOSE: permutation\nhead(gsea_res)\ntail(gsea_res)\nsaveRDS(gsea_res,'data/4_2_gsea_GO.rds')\n\n## REACTOME ####\ngsea_res <- gsePathway(geneList, \n                pvalueCutoff = 1)\nhead(gsea_res)\nsaveRDS(gsea_res,'data/4_2_gsea_RE.rds')\n```\n:::\n\n\n### ③ visualization\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 3. visualization ####\ngsea_res <- readRDS('data/4_2_gsea_GO.rds')\n## 3-1. Dot Plot (summary) ####\n# x: GeneRatio, dot size: Gene Count, color: p.adjust\ndotplot(gsea_res, showCategory = 5, split = \".sign\") + \n  ggtitle(\"GO BP Enrichment\")\n\ndotplot(gsea_res, showCategory = 5, split = \".sign\") + \n  facet_grid(.~.sign) + # Up/Down Pathway \n  ggtitle(\"GO BP Enrichment (split)\")\n\n\n## 3-2. GSEA Enrichment Plot (for the specific pathway) ####\np1 <- gseaplot2(gsea_res, geneSetID = 1, title = gsea_res$Description[1])\nprint(p1)\n\n### multiple (Top 3)\np2 <- gseaplot2(gsea_res, geneSetID = 1:3)\nprint(p2)\n\n# 3-3. Ridge Plot (expression level), requring ggridges\nridgeplot(gsea_res, showCategory = 5) + labs(x = \"Enrichment Distribution\")\n\n# 3-4. Network plot\ncnetplot(gsea_res, foldChange=geneList)\n```\n:::\n\n\n![](/assets/img/postDEG/GSEA.png){fig-align=\"center\"}\n\n### ④ exploring anything of interest\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 4. Core Enrichment Genes ####\ngsea_res_df <- as.data.frame(gsea_res)\nhead(gsea_res_df)\n\n# save the result\nwrite.csv(gsea_res_df, \"data/4_2_gsea_GO_results.csv\")\n\n# show core genes for a pathway\ncore_genes <- strsplit(gsea_res_df$core_enrichment[1], \"/\")[[1]]\nprint(core_genes)\n\n\n# 5. 자유 탐색 아이디어: 관심 유전자 탐색 ####\n# 특정 유전자가 종양에서 어떻게 변하는지 확인\nmy_gene <- \"EGFR\"  # 원하는 유전자로 바꿔보세요\nmy_gene <- \"ARHGAP44\"  # 원하는 유전자로 바꿔보세요\n\n# 발현량 시각화\nplotCounts(dds, gene = my_gene, intgroup = \"condition\", returnData = FALSE)\n\n# 더 예쁘게\nplot_data <- plotCounts(dds, gene = my_gene, intgroup = \"condition\", returnData = TRUE)\nplot_data$count %>% head\nplot_data$count %>% max\ncounts(dds,normalized=T)[my_gene,] %>% max\nlog2(counts(dds,normalized=T)[my_gene,] %>% max)\n\nggplot(plot_data, aes(x = condition, y = log2(count+1), fill = condition)) +\n  geom_boxplot() +\n  geom_jitter(width = 0.2, alpha = 0.5) +\n  labs(title = paste0(my_gene, \" Expression\"),\n       y = \"log2(Normalized Counts)\") +\n  theme_minimal()\n```\n:::\n\n\n![](/assets/img/postDEG/box.png){fig-align=\"center\"}\n\n------------------------------------------------------------------------\n\n# 전체 분석 파이프라인\n\n![](/assets/img/summary.png){fig-align=\"center\"}\n\n------------------------------------------------------------------------\n\n## ➕ references\n\n### EDA & Transformation\n\n[vst / rlog: Anders & Huber, Genome Biology 2010](https://doi.org/10.1186/gb-2010-11-10-r106)\\\n[DESeq2 Vignette — Data transformations](https://bioconductor.org/packages/release/bioc/vignettes/DESeq2/inst/doc/DESeq2.html#data-transformations-and-visualization)\\\n[StatQuest: PCA step-by-step](https://www.youtube.com/watch?v=FgakZw6K1QQ)\n\n### DESeq2\n\n[DESeq2: Love, Huber & Anders, Genome Biology 2014](https://doi.org/10.1186/s13059-014-0550-8)\\\n[DESeq2 Vignette (Bioconductor)](https://bioconductor.org/packages/release/bioc/vignettes/DESeq2/inst/doc/DESeq2.html)\\\n[RNA-seq workflow: gene-level EDA & DEG (F1000Research)](https://doi.org/10.12688/f1000research.7035.2)\\\n[LFC shrinkage — apeglm: Zhu, Ibrahim & Love, Bioinformatics 2019](https://doi.org/10.1093/bioinformatics/bty895)\\\n[Design matrix guide: Law et al., F1000Research 2020](https://doi.org/10.12688/f1000research.27893.1)\n\n### Multiple Testing\n\n[BH procedure: Benjamini & Hochberg, JRSS-B 1995](https://doi.org/10.1111/j.2517-6161.1995.tb02031.x)\\\n[Independent filtering: Bourgon, Gentleman & Huber, PNAS 2010](https://doi.org/10.1073/pnas.0914005107)\\\n[StatQuest: FDR and multiple testing](https://www.youtube.com/watch?v=K8LQSvtjcEo)\n\n### Visualization\n\n[EnhancedVolcano (Bioconductor)](https://bioconductor.org/packages/release/bioc/vignettes/EnhancedVolcano/inst/doc/EnhancedVolcano.html)\\\n[ComplexHeatmap: Gu et al., Bioinformatics 2016](https://doi.org/10.1093/bioinformatics/btw313)\\\n[ComplexHeatmap Complete Reference](https://jokergoo.github.io/ComplexHeatmap-reference/book/)\n\n### GSEA\n\n[GSEA: Subramanian et al., PNAS 2005](https://doi.org/10.1073/pnas.0506580102)\\\n[Mootha et al., Nature Genetics 2003](https://doi.org/10.1038/ng1180)\\\n[clusterProfiler 4.0: Wu et al., The Innovation 2021](https://doi.org/10.1016/j.xinn.2021.100141)\\\n[clusterProfiler Book (Yu Lab)](https://yulab-smu.top/biomedical-knowledge-mining-book/)\\\n[MSigDB](https://www.gsea-msigdb.org/gsea/msigdb)\\\n[Gene Ontology](https://geneontology.org/)\\\n[Reactome](https://reactome.org/)\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}