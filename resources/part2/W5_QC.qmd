---
title: "W5. Pre-processing"
author: "Eunji Ha"
date: last-modified
format:
  html
execute:
  echo: true
  eval: true
---

## Workflow of RNA-seq Data Analysis

The overall workflow of RNA-seq analysis is as follows (Conesa et al., *Genome Biol.*, 2016):

1)  Read trimming
2)  Alignment
3)  Quantification
4)  Normalization
5)  Differential analysis
6)  Functional profiling

![](/assets/img/part2/W5_QC.png){fig-align="center"}

------------------------------------------------------------------------

## 0) Quality Check: [FastQC](https://www.bioinformatics.babraham.ac.uk/projects/fastqc/)

![](/assets/img/part2/W5_QC.jpg){fig-align="center" width="734"}

-   **Per base sequence quality**:\
    check quality toward the `3' end`\
    verify that scores remain in the `green zone`
-   **Per sequence GC content**:\
    should follow a `normal distribution`\
    multiple peaks? ... contamination
-   **Adapter Content**: determines whether trimming is necessary
-   **Number of reads**: check `Q20/Q30 ratios`

------------------------------------------------------------------------

## 1) Read Trimming: [TrimGalore](https://github.com/FelixKrueger/TrimGalore)

![](/assets/img/part2/W6_trim.png){fig-align="center" width="398"}

``` bash
# TrimGalore key functions
# 1) Remove low quality reads (Phred score Q20, error rate 1%) - sliding window
# 2) Remove adapter sequences (Illumina Universal, TruSeq/Nextera)
# 3) Minimum length filtering - discard reads that are too short
```

------------------------------------------------------------------------

## 2) Alignment: STAR

-   **Bowtie2/BWA**: for DNA; indexing & mismatch-based
-   [**STAR**](https://github.com/alexdobin/STAR): spliced aligner dedicated to RNA-seq
    -   Recognizes splice junctions → maps to exons only
    -   Requires high RAM (\>30GB)

![](/assets/img/part2/W6_star.png){fig-align="center" width="303"}

-   **QC criterion (general)**: %Uniquely mapped reads \> 80%, #mapped reads \> 20M
-   **Output format**: SAM (text) or [BAM (binary)]{.underline}
-   [**Galaxy**](https://galaxy-main.usegalaxy.org/)**:** web-based tool for pre-processing

![](/assets/img/part2/W5_galaxy.png){fig-align="center"}

------------------------------------------------------------------------

## 3) Quantification

### Alignment-based (STAR + featureCounts)

![](/assets/img/part2/W5_STAR.gif){fig-align="center" width="590"}

-   Traditional approach: position indexing → mapping to reference
-   Requires BAM files; computationally intensive

### Alignment-free / Pseudo-alignment ([Salmon](https://salmon.readthedocs.io/en/latest/salmon.html), Kallisto)

![](/assets/img/part2/W5_salmon.png){fig-align="center" width="333"}

-   Uses short **k-mer (k=31)** compatibility checks instead of full read mapping

-   No BAM required; very fast

-   Multi-mapping reads → isoform-level probabilities assigned via **EM algorithm**

    -   e.g., read A → isoform X (0.6), isoform Y (0.4)

::: callout-tip
## tximport using salmon outputs (transcripts)

1)  aggregation: transcripts –\> gene
2)  correction: average transcript length per sample (isoform switching) –\> gene count correction
3)  import: float number –\> integer
:::

------------------------------------------------------------------------

## 4) Filtering and Normalization

### Filtering examples [(depending on your study)]{.underline}

-   **Remove low-count genes**: remove genes with zero counts in ≥ N×`0.5` samples (N: the smallest group)

![](/assets/img/part2/W5_filter.png){fig-align="center" width="565"}

-   **Remove confounding genes**:
    -   Mitochondrial (`^MT-`)
    -   Ribosomal (`^RPS|^RPL`)
    -   Immunoglobulin (`^IG[HKL]`)

------------------------------------------------------------------------

### Why Normalization is Needed

-   **Library size (depth)**: higher sequencing depth → higher counts

![](/assets/img/part2/W5_library.png){fig-align="center" width="689"}

-   Gene length: longer genes → higher counts

![](/assets/img/part2/W5_depth.jpg){fig-align="center" width="519"}

------------------------------------------------------------------------

### Normalization Methods Comparison

| Method | Correction | Comparison | Remarks |
|------------------|------------------|------------------|-------------------|
| Raw Count | × | × | edgeR/DESeq2 input |
| RPKM/FPKM | Depth + Length | Intra-sample | Deprecated |
| TPM | Depth + Length | Intra + Inter-sample | per-sample sum = 1M |
| CPM | Depth | Inter-sample |  |
| TMM/RLE | Depth + Composition | Inter-sample | Standard for DEG analysis |

------------------------------------------------------------------------

### Practice: Normalization in R

```{r}
library(edgeR)
library(DESeq2)
library(tidyverse)
library(ggplot2)

set.seed(123)

# 1. Generate fake data (1000 genes × 6 samples) ----
n_genes <- 1000; n_samples <- 6
sample_names <- c(paste0("Ctrl_", 1:3), paste0("Treat_", 1:3))
gene_names <- paste0("Gene_", 1:n_genes)

## 1-2. creating Raw Count Matrix (Negative Binomial distribution) ####
raw_counts <- matrix(rnbinom(n_genes * n_samples, mu = 100, size = 5), # mean: 100, dispersion: 5
                     nrow = n_genes) # making 1000 genes
raw_counts[, 4:6] <- raw_counts[, 4:6] * 3  # varying library size
colnames(raw_counts) <- sample_names
rownames(raw_counts) <- gene_names
head(raw_counts)

gene_lengths <- round(runif(n_genes, min = 200, max = 5000))
head(gene_lengths)
```

------------------------------------------------------------------------

```{r}
# 2. CPM (counts per million) ####
## library size: Total Counts per sample 
lib_sizes <- colSums(raw_counts)
head(lib_sizes)
cpm_counts <- sweep(raw_counts, 
                    2, # 1: by row (gene), 2: by column (sample)
                    lib_sizes, "/") * 1e6
head(cpm_counts)
raw_counts[1,1]/lib_sizes[1]* 1e6 # cpm_counts[1,1]


# 3. RPKM (reads per killobase million) ####
## gene length (kb)
kb_lengths <- gene_lengths / 1000

## calculating RPKM
# 3-1. correcting for gene length (RPK) ####
rpk <- sweep(raw_counts, 1, kb_lengths, "/")
head(rpk)
# 3-2. correcting for Library Size (million-based) (RPKM) ####
rpkm_counts <- sweep(rpk, 2, lib_sizes, "/") * 1e6
head(rpkm_counts)

# 4. TPM (transcripts per million) ####
## 4-1. RPK (Reads Per Kilobase) ####
rpk <- sweep(raw_counts, 1, kb_lengths, "/")

## 4-2. per-sample RPK ####
rpk_sums <- colSums(rpk)
head(rpk_sums)
## 4-3. calculating TPM (correcting for per-sample RPK) ####
tpm_counts <- sweep(rpk, 2, rpk_sums, "/") * 1e6
colSums(tpm_counts)  # each sample sums to 1M
colSums(rpkm_counts)
```

------------------------------------------------------------------------

```{r}
# 5. TMM from edgeR ####
# 5-1. make edgeR object (DGEList) ####
dge <- DGEList(counts = raw_counts)
# 5-2. make Normalization Factor from TMM ####
dge2 <- calcNormFactors(dge, method = "TMM")
## 5-3. normalizing counts (CPM, but using TMM factor) ####
tmm_counts <- cpm(dge2, normalized.lib.sizes = TRUE)
head(tmm_counts)

cpm_counts2 <- cpm(dge2, normalized.lib.sizes = FALSE)
head(cpm_counts2)
```

------------------------------------------------------------------------

```{r}
# 6. RLE from DESeq2 ####
## 6-1. metadata for DESeq2 object ####
colData <- data.frame(condition = factor(rep(c("Ctrl", "Treat"), each = 3)))
rownames(colData) <- sample_names
## 6-2. make DESeqDataSet (dds) ####
dds <- DESeqDataSetFromMatrix(countData = raw_counts,
                              colData = colData,
                              design = ~ condition)
dds
## 6-3. estimating Size Factor ####
dds2 <- estimateSizeFactors(dds)
dds2 # creating sizeFactor in colData
dds2@colData

## 6-4. normalized counts ####
deseq_counts <- counts(dds2, normalized = TRUE)
head(deseq_counts)
raw_counts2 <- counts(dds2, normalized = FALSE)
head(raw_counts2)
```

------------------------------------------------------------------------

```{r}
# 7. visualization ####
## 7-1. reshaping counts (Long format for ggplot) ####
transform_to_long <- function(count_matrix, method_name) {
  as.data.frame(count_matrix) %>%
    rownames_to_column("Gene") %>%
    pivot_longer(-Gene, names_to = "Sample", values_to = "Count") %>%
    mutate(Method = method_name,
           # Log2 transformation (pseudocount: +1 for 0)
           LogCount = log2(Count + 1))
}

## applying the function ####
transform_to_long(raw_counts, "1. Raw") %>% head
transform_to_long(cpm_counts, "2. CPM") %>% head

df_all <- rbind(
  transform_to_long(raw_counts, "1. Raw"),
  transform_to_long(cpm_counts, "2. CPM"),
  transform_to_long(rpkm_counts, "3. RPKM"),
  transform_to_long(tpm_counts, "4. TPM"),
  transform_to_long(tmm_counts, "5. TMM (edgeR)"),
  transform_to_long(deseq_counts, "6. DESeq2")
)
head(df_all)
tail(df_all)
```

------------------------------------------------------------------------

```{r}
## 7-2. visualization (Boxplot) ####
df_all %>%
ggplot(aes(x = Sample, y = LogCount, fill = Sample)) +
  # box plot
  geom_boxplot() + 
  # plot by method
  facet_wrap(~Method, scales = "free_y") + 
  # theme
  theme_bw() + 
  # x-axis text
  theme(axis.text.x = element_text(angle = 45, hjust = 1)) + 
  # plot title
  labs(title = "Normalization Method Comparison",
       y = "Log2(Counts + 1)",
       x = "")
```

------------------------------------------------------------------------

### TMM vs RLE

-   **TMM**: selects a reference sample, trims extreme values, and normalizes based on a set of housekeeping genes
-   **RLE**: uses the median ratio of each gene relative to its geometric mean across samples as the size factor

![](/assets/img/part2/W5_TMM_RLE.jpg){fig-align="center" width="731"}

------------------------------------------------------------------------

## 📝 Week 5 Assignment

> Nothing. Please just submit a screenshot of your screen.

------------------------------------------------------------------------

## References {.smaller}

-   [SAM/BAM](https://hhj6212.github.io/biology/tech/2020/08/26/Bioinformatics-fileformats.html)
-   [Normalization comparison](https://academic.oup.com/bib/article/14/6/671/189645)
